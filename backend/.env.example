# copy to .env and fill in real values. never commit .env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/voice_ai
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# supabase project base url (jwks verification, recommended for new projects)
SUPABASE_URL=
# OR legacy hs256 jwt secret (older projects only)
SUPABASE_JWT_SECRET=
